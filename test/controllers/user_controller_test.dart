
import 'package:firebase_core/firebase_core.dart';
import 'package:happ_eats/controllers/user_controller.dart';
import 'package:happ_eats/firebase_options.dart';
import 'package:test/test.dart';

void main() {






  /*
  group('Test Name Validation', ()  {

    test('Names composed of uppercase and lowercase letters should be allowed', () async {
      await Firebase.initializeApp(
        options: DefaultFirebaseOptions.currentPlatform,
      );
      UsersController userController = UsersController();
    });




    test('Names composed of uppercase and lowercase letters should be allowed', () {
      expect(userController.validateName('Pepe'), null);
    });

    test('Names should be able to uses spaces', () {
      expect(userController.validateName('Amtonio Javier'), null);
    });

    test('Names should be able to use apostrophes', () {
      expect(userController.validateName("L'Garde"), null);
    });

    test('Names should be able to use accents', () {
      expect(userController.validateName("Céleste "), null);
    });

    test('Names should be able to use hyphens', () {
      expect(userController.validateName("Boursier-Mougenot"), null);
    });

    test('Names should be able to use numbers', () {
      expect(userController.validateName("James 5"), null);
    });

  });*/
}